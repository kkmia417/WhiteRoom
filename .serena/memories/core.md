# Core

- Unity project root contains `Assets/`, `Packages/`, `ProjectSettings/` with scenes in `Assets/Scenes`.
- Current checked scenes are minimal: `Title.unity` and `Main.unity` contain only camera/light baseline objects.
- Game-specific source folders `Assets/Scripts` and `Assets/Class` exist but are empty at onboarding time.
- Read `mem:tech_stack` for Unity/package details, `mem:conventions` for local style, `mem:suggested_commands` for Windows commands, and `mem:task_completion` before closing coding tasks.
